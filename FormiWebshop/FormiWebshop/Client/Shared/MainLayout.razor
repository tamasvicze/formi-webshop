@inherits LayoutComponentBase
@inject ILocalStorageService LocalStorage
@inject AuthenticationStateProvider AuthenticationStateProvider
@inject NavigationManager NavigationManager
@inject ICartService CartService
@inject IStringLocalizer<App> Localizer

<MudThemeProvider />
<MudDialogProvider />
<MudSnackbarProvider />

<MudLayout>
    <MudAppBar>
        <MudButton Variant="Variant.Filled" Color="Color.Primary" Class="mr-3 px-5"><HomeButton /></MudButton>
        <Search/>
        <UserButton/>
        <CartCounter/>
        <CultureSelector/>
    </MudAppBar>
    <MudMainContent>
        <MudToolBar Class="pa-4 mud-theme-primary">
            <ShopNavMenu></ShopNavMenu>
        </MudToolBar>
        @Body
    </MudMainContent>
</MudLayout>